import SetupClient from "./SetupClient";

export const metadata = {
  title: "Setup Your Keychain | TACTUS",
  description: "Configure your TACTUS smart keychain with your favorite Spotify track.",
};

export default function SetupPage() {
  return <SetupClient />;
}
